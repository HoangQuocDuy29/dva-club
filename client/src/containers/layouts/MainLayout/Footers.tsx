import React from "react";
import { Box, Typography } from "@mui/material";

const Footers: React.FC = () => (
  <Box component="footer" sx={{ py: 0, textAlign: "center", bgcolor: "#f5f5f5" }}>
    
  </Box>
);

export default Footers;
